1. 나이를 입력한다.
그리고 20살 미만이면 아이, 20살 이상이면 성인이라고 출력한다.

2. 1번에 대해서 0 미만의 숫자인 경우는 잘못된 것이라는 메시지 예외처리 추가한다.

3. 숫자를 입력한다. 양수 , 0, 음수 중 하나를 출력한다.

hint) 삼항연산자는 중첩이 된다. (괄호로 묶기 등을 이용해서 시도해보기)

내가 푼 것
	//1.
	int age;
	printf("나이를 입력한다.\n");
	scanf_s("%d", &age);
	age>=20 ? printf("성인") : printf("아이");
	
	//2.
	age < 0 = printf("잘못된 메세지 입니다.");
	