#include<stdio.h>
int main()
{
	//switch case 문-java, c#, js 다 같음
	//실수와 실수 사이의 범위를 측정불가

	//else와 default 유사.
	//숫자가위바위보게임 만들기 가능(3으로 나눴을때 나머지를 이용ex>100->1-바위)
	//break 안적으면 그 위의 케이스는 같음

	return 0;
}
